sh-run

- 当前执行包：（例：声音复刻 MVP 执行包 v1，或直接粘贴执行包内容）
- 本轮主目标：（例：完成音频上传 → 第三方 API 调用 → 配音文件生成的完整链路）
- 本轮明确不做：（例：不做前端 UI 优化、不做错误重试机制、不做批量任务队列）
- 验收标准：（例：本地跑通一条音频生成链路，输出文件可播放）
- 哪些情况必须停下来汇报：（例：需要引入新依赖、核心数据结构需要变更、API 方案需要切换）
